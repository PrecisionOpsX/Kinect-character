# React + Vite

run npm install to install all node modules

To start the app:

- run this command in one terminal
'node ./debug-capture-server/streamData.js'

- run this command in another terminal
'npm run dev --silent'
